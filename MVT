//AIM: A program to simulate the MVT.

#include<stdio.h>

#include<conio.h>

void main()

{ int m=0,m1=0,m2=0,p,count=0,i; clrscr(); printf("enter the memory capacity:"); scanf("%d",&m); printf("enter the no of processes:"); scanf("%d",&p); for(i=0;i<p;i++)

if(m1<=m)

{ printf("\nenter memory req for process%d: ",i+1); scanf("%d",&m1); count=count+m1; { if(count==m) printf("there is no further memory remaining:"); printf("the memory allocated for process%d is: %d ",i+1,m); m2=m-m1; printf("\nremaining memory is: %d", m2); m=m2;

} } else {

1

CSE0613206_Lab Sheet-7 Operating System Lab

printf("memory is not allocated for process%d", i+1);

} printf("\nexternal fragmentation for this process is:%d", m2); } getch();

}
